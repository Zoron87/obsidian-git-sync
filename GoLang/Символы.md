var c rune = '#'
```go
func main() { 
	var char rune 
	fmt.Scanf("%c", &char) 
	fmt.Println("Вы ввели:", string(char)) }
```

`fmt.Scanf` — это функция, которая позволяет считывать ввод с консоли по определённому формату. Если простыми словами, ты заранее говоришь программе, что именно ты хочешь прочитать: символ, число, строку и так далее.

**`"%c"`** — это формат, который говорит функции: "Мы хотим прочитать один символ". Буква `c` comes от слова "character" (символ).

#### Способ 2: Чтение одного символа через `bufio`

```go
package main

import (
    "bufio"
    "fmt"
    "os"
)

func main() {
    reader := bufio.NewReader(os.Stdin)
    char, _, _ := reader.ReadRune()
    fmt.Println("Вы ввели:", string(char))
}
```
1. `bufio.NewReader(os.Stdin)` - создает буферизованный читатель для консоли
2. `reader.ReadRune()` - считывает **один символ** (даже если это эмодзи или кириллица)
3. `string(char)` - преобразует код символа обратно в читаемый вид
### Представление символов в компьютере
```go
import "fmt" 
func main() { 
	c := '#' 
	code := int(c) 
	fmt.Println(code) // 35 }
```

### Сравнение символов

Символы можно сравнивать — сравниваются их **коды**:

```go
package main

import "fmt"

func main() {
    first := 'B'
    second := 'v'
    fmt.Println(first < second) // true
}
```

Здесь `'B'` действительно меньше `'v'`, так как имеет меньший код.

Проверка, **является ли символ заглавной латинской буквой**:

```go
package main

import "fmt"

func main() {
    var c rune = 'B'
    if c >= 'A' && c <= 'Z' {
        fmt.Println("Заглавная буква английского алфавита")
    }
}
```

### Арифметические операции

С символами можно выполнять арифметические операции. При этом к символу автоматически применяется его числовой код, и операция происходит над этим числом. Поскольку символы представляются целыми числами, с ними можно выполнять те же арифметические действия, что и с обычными целыми числами.  
Например:

```go
package main

import "fmt"

func main() {
    var c rune = 'a'
    result := c + 3
    fmt.Println(string(result)) // "d"
}
```

### Символы в циклах

Благодаря порядку кодов, символы удобно использовать в циклах. Например, вывод всех заглавных латинских букв:

```go
package main

import "fmt"

func main() {
    // Вывод всех заглавных латинских букв
    for w := 'A'; w <= 'Z'; w++ {
        fmt.Println(string(w))
    }
}
```

Здесь переменная `w` — это символ, и на каждой итерации к нему прибавляется _1_ (по коду).