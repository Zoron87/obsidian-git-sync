Если объект находится в неподходящем состоянии для вызова метода, это классический случай `InvalidOperationException`, то есть **ошибка состояния объекта**. 

Аргументы могут быть корректные, но операция недопустима в текущем состоянии объекта.